# Westone

Full-stack Westone project organized for GitHub.

## Structure
- `frontend/` — Frontend application
- `backend/` — Backend/API application

## Environment
Create the required `.env` files locally from the project's `.env.example` files (if provided). Never commit secrets.
